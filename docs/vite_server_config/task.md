# タスク: Vite 開発サーバーのネットワーク公開設定

## 目的
開発サーバーをローカルホスト以外（LAN 内の他のデバイスなど）からもアクセスできるように、全てのネットワークインターフェースで公開する。

## サブタスク
- [x] `vite.config.ts` に `server` セクションを追加。
- [x] `host: true` を設定してネットワーク公開を有効化。
- [x] `port: 5173` を明示的に指定。
