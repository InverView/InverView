# タスクリスト：下スワイプアニメーションの改善およびミニプレーヤー動画表示バグの修正

- [x] **1. ミニプレーヤーでの動画表示バグの修正**
  - [x] `VideoPlayer.tsx` において、`isMini === true` の際の `globalVideoHost.style.zIndex` を `46` に変更する。
- [x] **2. スワイプキャンセル時および遅いスワイプ時のカクつき改善 (ゼロ・リレンダリング方式への移行)**
  - [x] `WatchPage.tsx` に `pageContainerRef` および `progressBarRef` を導入。
  - [x] スワイプ中のタッチムーブにおいて React のステート更新を完全に廃止し、DOMの直接更新（`transform`, `opacity`, `borderRadius` の `style` 上書き）に移行。これによりリレンダリングオーバーヘッドを完全回避。
  - [x] 指を離してキャンセルした際に、`resetSwipeStyles` により transition を一時適用して滑らかに `0` に戻す DOM 補間処理を実装。
- [x] **3. 中間点到達時のアニメーションの本格実装 (最大化 → ミニ)**
  - [x] スワイプのコミットを非同期に処理し、アニメーション（300ms）完了後に `minimizeToMobilePlayerAndGoBack()` を実行する `commitSwipeAndMinimize` 関数を実装。
  - [x] コミット状態への移行時に DOM を直接操作し、画面外への縮小スライドフェードアウトアニメーションを滑らかに実行する。
- [x] **4. ミニプレーヤーから最大化プレーヤーへの拡大移行アニメーションの実装 (ミニ → 最大化)**
  - [x] `MiniPlayer.tsx` 内のモバイル用のスワイプ追従を DOM 直接操作に移行。
  - [x] 上スワイプの中間点（-80px）またはタップによる拡大確定時、ミニプレーヤーカード自体が `translateY(-120px) scale(1.15)` に向かって滑らかに拡大消失するプレミアムコミットアニメーション（250ms）を実装。
  - [x] `WatchPage.tsx` において、マウントおよびロード完了時にページ全体を `translateY(100vh) scale(0.7)` から `translateY(0px) scale(1)` に向かって下からスッと吸い上がるように滑らかにフェードイン（拡大登場）させるアニメーションを実装。
- [x] **5. 動作検証**
  - [x] ミニプレーヤーでの動画再生表示の確認。
  - [x] ゆっくりと遅くスワイプした際にも一切ジッターやコマ落ちが発生しないことの確認。
  - [x] 相互遷移時（縮小・拡大）に、繋ぎ目を感じさせないシームレスでプレミアムなアニメーションが機能することの確認。
